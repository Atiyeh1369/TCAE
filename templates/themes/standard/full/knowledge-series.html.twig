{{ include("@ezdesign/full/header.html.twig") }}
</head>
<body>
    <div class="container">
        <div class="container d-inline-block">
            <div class=" row text-center py-5">
                {% for item in items %}
                <div class="col-md-4 mb-5">
                    <div class="card shadow" style="height: 100%">
                            <img src="{{ ez_field_value(item,'image').uri }}" width="100%"><br>
                            <h5>{{ ez_field_value(item,'title') }}</h5><br>
                            <p>{{ ez_field_value(item,'text') }}</p><br>
                            <a class="btn btn-outline-danger ml-3" href="{{ path('ez_urlalias', {'contentId': item.contentInfo.id}) }}" style="width: 35%">Read more</a><br>
                    </div>
                </div>
                {% endfor %}
            </div>
        </div>
    </div>
</body>